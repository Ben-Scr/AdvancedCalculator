<UserControl x:Class="AdvancedCalculator.Views.Controls.TitleBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework">
    <Grid Background="Transparent"
          MouseLeftButtonDown="OnDrag"
          Height="44">

        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Margin="0,0,8,0"
                    shell:WindowChrome.IsHitTestVisibleInChrome="True">
            <Button Width="36" Height="24" Margin="4,0" Content="─" Click="Minimize_Click" Style="{StaticResource DefaultButton}"/>
            <Button Width="36" Height="24" Margin="4,0" Content="▢" Padding="0" Click="MaximizeRestore_Click" Style="{StaticResource DefaultButton}"/>
            <Button Width="36" Height="24" Margin="4,0" Content="✕" Padding="0" Click="Close_Click" Style="{StaticResource DangerButton}"
                    Foreground="{StaticResource Foreground.Primary}"/>
        </StackPanel>
    </Grid>
</UserControl>
